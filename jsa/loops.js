// for loop

numbers = [1, 2, 3, 4, 5, 6];

 for(array_index = 0; array_index < numbers.length; array_index++){
    console.log(numbers[array_index]);
 }

 //find sumof numbers in sn array
 const num = [2, 3, 4, 5, 6, 7, 8, 9, 10]

 let sum_of_nums = 0

 for(let num_index = 0; num_index< num.length; num_index++){

    sum_of_nums = sum_of_nums + num[num_index];
 }
 console.log(sum_of_nums);

 //while loop example
 let fruit = ["mango", "guava", "apple"];

 let fruit_index = 0;
 while(fruit_index<fruit.length){

    console.log(fruit[fruit_index])

 }