function function_name(){

    //body
}
//sum of two numbers
function add_two_numbers(x, y){
const sum = x + y;
console.log(sum);

}
add_two_numbers(4, 6);

//returning function example
function calculate_age(){
    
    const year_of_birth=2010;

    const age = 2024-year_of_birth;
    return age; 
    return year_of_birth;

}
 console.log(calculate_age());

 //returning function with parameters
 function calculate_students_grade(student_name, student_class){
   let student_grade = "";
    if(marks > 90){
        student_grade="d1";
        
    }

 }